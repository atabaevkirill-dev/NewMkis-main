Read `/AGENTS.md` and `/docs/PROJECT_CONTEXT.md` before suggesting or changing code. The application is `/desktop` (Tauri 2 + Rust + React + TypeScript). Preserve the cockpit UX rules, service-protocol-only TL.0009 movement, keychain credential storage and the stated implementation boundary.
